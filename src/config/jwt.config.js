module.exports = {
  secret: process.env.TOKEN_KEY,
  // expires: "2h", // set your own expiration time
  // refreshSecret: process.env.REFRESH_TOKEN_SECRET,
  // refreshExpires: "7d", // set your own refresh expiration time
};
